services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: trendplan
      POSTGRES_PASSWORD: trendplan
      POSTGRES_DB: trendplan
    ports:
      - "5432:5432"
    volumes:
      - trendplan_pg:/var/lib/postgresql/data

volumes:
  trendplan_pg:
