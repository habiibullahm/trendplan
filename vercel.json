{
  "buildCommand": "prisma generate && npm run db:deploy:retry && next build",
  "framework": "nextjs",
  "crons": [
    {
      "path": "/api/cron/reminders",
      "schedule": "0 13 * * *"
    }
  ]
}
